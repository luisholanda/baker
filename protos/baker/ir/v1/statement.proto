syntax = "proto3";

package baker.ir.v1;

import "baker/ir/v1/values.proto";

message Block {
  repeated Statement statements = 1;
}

message Statement {
  oneof statement {
    Value return = 2;
  };
}
