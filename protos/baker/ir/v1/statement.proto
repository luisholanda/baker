syntax = "proto3";

package baker.ir.v1;

import "baker/ir/v1/values.proto";

message Block {
  repeated Statement statements = 1;
}

message Statement {
  oneof statement {
    string raw = 1;
    Value return = 2;
  };
}
